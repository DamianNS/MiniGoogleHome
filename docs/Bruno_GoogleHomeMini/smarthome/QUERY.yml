info:
  name: QUERY
  type: http
  seq: 2

http:
  method: POST
  url: "{{server}}/api/smarthome"
  body:
    type: json
    data: |-
      {
        "RequestId":"1",
        "Inputs":[
          {
            "Intent":"action.devices.QUERY"  
          }
          
        ]
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
