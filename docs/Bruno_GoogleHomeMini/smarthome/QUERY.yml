info:
  name: QUERY
  type: http
  seq: 2

http:
  method: POST
  url: "{{server}}/api/smarthome"
  body:
    type: json
    data: |-
      {
        "RequestId": "14515786580954270871",
        "Inputs": [
          {
            "Intent": "action.devices.QUERY",
            "Payload": {
              "devices": [
                {
                  "id": "pi_media_speaker_001",
                  "customData": {}
                }
              ],
              "commands": []
            }
          }
        ]
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
