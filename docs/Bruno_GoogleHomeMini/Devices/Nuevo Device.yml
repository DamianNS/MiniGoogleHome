info:
  name: Nuevo Device
  type: http
  seq: 2

http:
  method: POST
  url: http://localhost:5134/api/devices
  body:
    type: json
    data: |-
      {
        "Nombre": "Patoclo"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
