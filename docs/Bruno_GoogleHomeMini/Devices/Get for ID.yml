info:
  name: Get for ID
  type: http
  seq: 3

http:
  method: GET
  url: "{{server}}/api/devices/1"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
